{{! Filter By }}

<div class="filter">
  <BsDropdown  data-test-id="bsDropdown"  as |dd|>
    <dd.toggle class="filter-by" data-test-id="filterBy"><span data-test-id="hasFilterBy">Filter by</span><p
        class="emp"
        type="button"
        {{action "filterby"}} 
      >{{if currentTeam currentTeam "All Employees"}}</p>
      <img
        src="/assets/images/accordion-open.svg"
        alt="img"
        class="open {{if clicked 'turn'}}"
      />
    </dd.toggle>
    <dd.menu>
      <li class="team" data-test-id="filterCurrentTeam">Teams</li>
      {{#each myTeamMembers as |myTeam|}}
        <li
          class="filter-table"
          {{action "isFilter" myTeam.team}}
           
        >{{myTeam.team}}</li>
      {{/each}}
    </dd.menu>
  </BsDropdown>
</div>

{{! Sort By }}

<div class="sort">
  <BsDropdown as |dd|>
    <dd.toggle class="filter-by"><span>Sort by</span><p
        class="emp"
        {{action "sortby"}}
      >{{if sortDisplay sortDisplay "First Name"}}</p>
      <img
        src="/assets/images/accordion-open.svg"
        alt="img"
        class="open {{if sortByclicked 'turn'}}"
      />
    </dd.toggle>
    <dd.menu>
      <div class="menu">
        {{#each sortArray as |sortType|}}
          <li
            class="sort-table
              {{if (eq this.selectedValue sortType.name) 'tick'}}"
            {{action "sortFunc" sortType.name sortType.displayName}}
          >{{sortType.displayName}}</li>
        {{/each}}
        <div class="dropdown-divider divider"></div>
        {{#each sortOrderArray as |sortOrderType|}}
          <li
            class="sort-method
              {{if (eq selectedOrder sortOrderType.orderParam) 'tick'}}"
            {{action "sortOrder" sortOrderType.orderParam}}
          >{{sortOrderType.sortOrder}}</li>
        {{/each}}
      </div>
    </dd.menu>
  </BsDropdown>
</div>

{{! Search Bar }}

<div class="search-field">
  {{input
    type="search"
    id="search"
    placeholder="Search by name"
    key-up="fname"
  }}
  <em>{{svg-jar "search" class="magnifier"}}</em>

</div>